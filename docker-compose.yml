version: '3.9'

services:
  housing-portal:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
      - "8080:8080"
      - "8081:8081"
    restart: unless-stopped
    environment:
      - PYTHONUNBUFFERED=1
